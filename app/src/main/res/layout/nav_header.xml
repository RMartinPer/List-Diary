<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#FFFFFF"
    android:orientation="vertical"
    android:padding="@dimen/nav_header_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Light">

    <ImageView
        android:id="@+id/nav_icon"
        android:layout_width="@dimen/nav_header_icon_side"
        android:layout_height="@dimen/nav_header_icon_side"
        android:contentDescription="@string/nav_header_desc"
        app:srcCompat="@drawable/logo_list" />

    <TextView
        android:id="@+id/nav_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/nav_header_spacing"
        android:layout_marginBottom="-4dp"
        android:includeFontPadding="false"
        android:text="@string/nav_header_title"
        android:textColor="#000000"
        android:textSize="18sp"
        android:textStyle="bold" />

</LinearLayout>